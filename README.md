# bookmarkd

Minimalist self-hosted bookmarking system with browser extension.

## Installation
Backend:
``` bash
cp env.template .env

# fill out .env

go run main.go
```

To install Firefox extension, download .xpi from release, go to `about:addon`
and choose `Install Add-on From File`.
